// import NewsItem from '../NewsItem/NewsItem';
// import { NWrapper } from './NewsList.styled';

const NewsList = ({ allnews }) => {
  return (
    <>
      {/* <NWrapper>
        {allnews.map((news, _id) => (
          <NewsItem friend={news} key={_id} />
        ))}
      </NWrapper> */}
    </>
  );
};
export default NewsList;
