import { Button } from "./addPetBtn.styled";

export const AddPetBtn = () => {
  return (
    <Button
      type="button"
      // onClick={() => onClick()}
    >
          Add Pet
      </Button>
  );
};
